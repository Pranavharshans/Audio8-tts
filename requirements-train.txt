-r requirements.txt
# Newer releases use a PyArrow fragment reader that can abort while streaming
# partially consumed remote Parquet shards (huggingface/datasets#7357).
datasets==3.1.0
accelerate>=1.10
tensorboard>=2.16
